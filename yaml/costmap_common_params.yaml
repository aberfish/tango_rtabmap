#plugins: 
#    - {name: static_map,       type: "costmap_2d::StaticLayer"} 
#    - {name: inflation,        type: "costmap_2d::InflationLayer"}
#    - {name: sonar,            type: "range_sensor_layer::RangeSensorLayer"}

inflation:
  inflation_radius: 1.0

#layer definitions
static:
  map_topic: /map
  subscribe_to_updates: false  

obstacles_laser:
  obstacle_range: 5.0
  raytrace_range: 5.0
  combination_method: 0
  observation_sources: scan
  scan: {data_type: LaserScan, topic: scan, marking: true, clearing: true, inf_is_valid: true, obstacle_range: 5, combination_method: 0, raytrace_range: 3}

robot_radius: 0.150
transform_tolerance: 30.0
origin_x: -5.0
origin_y: -5.0
